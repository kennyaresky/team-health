import s from "../../styles/home.module.css"

export default function Body() {
  return (
    <>
    <main className={`${s.main} *:rounded-2xl`}>
    <div className={`${s.main_top}`}>
    </div>
    <div className={`${s.side_bottom_left}`}>
    </div>
    <div className={`${s.side_bottom_right}`}>
    </div>
  </main>
    </>
  )
}
